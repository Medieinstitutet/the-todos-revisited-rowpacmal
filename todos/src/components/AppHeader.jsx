import { IconChecklist } from '@tabler/icons-react';

const AppHeader = () => (
  <header>
    <h1 className="align-icons">
      <IconChecklist size={'5rem'} />
      TODO List
    </h1>
  </header>
);

export default AppHeader;
